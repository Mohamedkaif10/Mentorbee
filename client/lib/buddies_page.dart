import 'package:flutter/material.dart';

class BuddiesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buddies'),
      ),
      body: Center(
        child: Text('Buddies Page Content'),
      ),
    );
  }
}
